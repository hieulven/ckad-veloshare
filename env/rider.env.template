# Secret: rider-db  (Deployment `rider`)
#
# Copy to env/rider.env and fill in. Gitignored; applied by `make secrets`.
# DB_PASSWORD must match RIDER_PASSWORD in env/postgres.env.

DB_HOST=postgres
DB_PORT=5432
DB_NAME=veloshare
DB_USER=rider
DB_PASSWORD=change-me-rider
DB_SCHEMA=riders
