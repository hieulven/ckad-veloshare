# Secret: trip-db  (Deployment `trip`)
#
# Copy to env/trip.env and fill in. Gitignored; applied by `make secrets`.
# DB_PASSWORD must match TRIP_PASSWORD in env/postgres.env.

DB_HOST=postgres
DB_PORT=5432
DB_NAME=veloshare
DB_USER=trip
DB_PASSWORD=change-me-trip
DB_SCHEMA=trips
