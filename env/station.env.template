# Secret: station-db  (Deployment `station`)
#
# Copy to env/station.env and fill in. Gitignored; applied by `make secrets`.
# DB_PASSWORD must match STATION_PASSWORD in env/postgres.env.

DB_HOST=postgres
DB_PORT=5432
DB_NAME=veloshare
DB_USER=station
DB_PASSWORD=change-me-station
DB_SCHEMA=stations
